import { delivery } from './delivery';

const occasionPicker = {
  render() {
    const html = `
      <div id="bi21-datepicker">
        <input id="bi21-date">
      </div>
    `;

    return html;
  },
};

export default occasionPicker;
