import { setup } from './services';

/**
 * {{GD046}} - {{Fresh Relevance}}
 */

const Run = () => {
  const Exp = {
    init: () => {
      setup();
    },
  };

  Exp.init();
};

export default Run;
