const searchInput = (id) => {
    const htmlStr = `<input type="text" class="${id}__search-input" placeholder="Search destination, postcode, or hotel">`;

    return htmlStr;
};
export default searchInput;
