const preCartPopupEl = document.querySelector('.preCartPopUp');
const preCartPopup = typeof preCartPopupEl !== 'undefined' ? preCartPopupEl : null;

const elements = {
    preCartPopup
};

export default elements;