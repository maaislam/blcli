const branchesContainers = document.querySelector('.area_branch_details');
const branches = document.querySelectorAll('.area_branch_details .address');

export default {
    branchesContainers,
    branches
};