const plpCard = (id) => {
  const htmlStr = `<div class='${id}__plpCard'>
        <div class='content'>
            <h3 class='title'>Get a FREE valve with this radiator</h3>
        </div>
    </div>`;
  return htmlStr;
};

export default plpCard;
