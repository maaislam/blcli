const seeFullRangeBtn = (id) => {
    const htmlStr = `<div class="${id}__see_full_range_btn">
                      <a href="https://www.specsavers.co.uk/glasses/all-glasses">
                      See the full range
                      </a>
                      </div>`;
  
    return htmlStr.trim();
  };
export default seeFullRangeBtn;
  